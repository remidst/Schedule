$('.calendar').fullCalendar('removeEvents', '<%= "appointment_#{@appointment.id}" %>' );
alert($.parseJson("<%= j render partial: 'appointments/appointment.json', locals: {appointment: @appointment} %>");

$('.calendar').fullCalendar(
  'renderEvent', 
  $.parseJSON("<%= j render partial: @appointment, formats: :json, locals: {appointment: @appointment}  %>"), 
  true
);
$('.modal').modal('hide');