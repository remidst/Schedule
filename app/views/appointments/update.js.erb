$('.calendar').fullCalendar('removeEvents', '<%= "appointment_#{@appointment.id}" %>' );
$('.calendar').fullCalendar(
  'renderEvent', 
  $.parseJSON("<%=j @appointment.to_json.html_safe %>"), 
  true
);
$('.modal').modal('hide');