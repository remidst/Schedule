$('.calendar').fullCalendar('removeEvents', '<%= "recurring_#{@recurring_appointment.id}" %>');
$('.modal').modal('hide');
$('.calendar').fullCalendar(
  'renderEvents', 
  $.parseJSON("<%= j render('recurring_appointments/recurring_appointment.json') %>"), 
  true
);